


<!doctype html>
<html ⚡ lang="en">
<head>
<meta charset="utf-8">
<script async src="https://cdn.ampproject.org/v0.js"></script>
<meta name="robots" content="index, follow, nosnippet">
<title>ASUPANTOTO | Link Situs Toto Togel dan Slot Online 4D Game Gacor Hari Ini 2026 Server Resmi Terbaik</title>
<link rel="canonical" href="https://edufest.politeknikcendana.ac.id/home/">
<link rel="icon" type="image/png" href="https://sikikea.studentdata.online/img/icon.png" />
<meta name="viewport" content="width=device-width,minimum-scale=1,initial-scale=1">
<meta name="theme-color" content="#F8F4FC">
<meta name="description" content="ASUPANTOTO merupakan situs toto togel dan slot gacor online yang merancang platform untuk pengguna supaya gampang menemukan link permainan gacor hari ini 2026. Dengan kualitas server nomor satu, provider resmi, dan proteksi keamanan akun 24 jam, Asupantoto menjadi tempat yang layak untuk bermain judi daring sepanjang hari.">
<meta property="og:title" content="ASUPANTOTO | Link Situs Toto Togel dan Slot Online 4D Game Gacor Hari Ini 2026 Server Resmi Terbaik">
<meta property="og:description" content="ASUPANTOTO merupakan situs toto togel dan slot gacor online yang merancang platform untuk pengguna supaya gampang menemukan link permainan gacor hari ini 2026. Dengan kualitas server nomor satu, provider resmi, dan proteksi keamanan akun 24 jam, Asupantoto menjadi tempat yang layak untuk bermain judi daring sepanjang hari.">
<meta property="og:type" content="website">
<meta property="og:image" content="https://sikikea.studentdata.online/img/Asupantoto.jpg">
<style amp-boilerplate>body{-webkit-animation:-amp-start 8s steps(1,end) 0s 1 normal both;-moz-animation:-amp-start 8s steps(1,end) 0s 1 normal both;-ms-animation:-amp-start 8s steps(1,end) 0s 1 normal both;animation:-amp-start 8s steps(1,end) 0s 1 normal both}@-webkit-keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}@-moz-keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}@-ms-keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}@-o-keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}@keyframes -amp-start{from{visibility:hidden}to{visibility:visible}}</style><noscript><style amp-boilerplate>body{-webkit-animation:none;-moz-animation:none;-ms-animation:none;animation:none}</style></noscript>
<style amp-custom>
        * { margin: 0; padding: 0; box-sizing: border-box; }

        :root {
        --ink: #0a0a0a;
        --paper: #f4f4f0;
        --white: #ffffff;
        --border: 3px solid var(--ink);
        }
        :root{
    --wallpaper: url('https://sikikea.studentdata.online/img/Asupantoto.jpg');
}

        body {
        font-family: 'Helvetica Neue', Arial, sans-serif;
        background: var(--paper);
        color: var(--ink);
        min-height: 100vh;
        padding: 24px 16px;
        overflow-x: hidden;
        position: relative;
        }

        /* === ANIMATED BG GRID === */
body::before{
    content:'';
    position:fixed;
    inset:0;
    background-image: var(--wallpaper);
    background-size: cover;
    background-position: center;
    filter: blur(25px);
    transform: scale(1.15);
    opacity:.6;
    z-index:-2;
}

        @keyframes drift {
        from { transform: translate(0, 0); }
        to { transform: translate(50px, 50px); }
        }

        /* === FLOATING SHAPES === */
        .shape {
        position: fixed;
        border: var(--border);
        background: var(--ink);
        z-index: 0;
        pointer-events: none;
        }

        .shape-1 {
        width: 80px;
        height: 80px;
        top: 8%;
        left: 6%;
        background: linear-gradient(180deg, #00e1ff 0%, #005c92 100%);
        animation: floatA 8s ease-in-out infinite;
        }

        .shape-2 {
        width: 60px;
        height: 60px;
        top: 70%;
        right: 8%;
        border-radius: 50%;
        background: linear-gradient(180deg, #00e1ff 0%, #005c92 100%);
        animation: floatB 10s ease-in-out infinite;
        }

        .shape-3 {
        width: 50px;
        height: 50px;
        bottom: 15%;
        left: 10%;
        background: linear-gradient(180deg, #00e1ff 0%, #005c92 100%);
        animation: floatC 12s ease-in-out infinite;
        transform: rotate(45deg);
        }

        @keyframes floatA {
        0%, 100% { transform: translate(0, 0) rotate(0deg); }
        50% { transform: translate(20px, -20px) rotate(15deg); }
        }
        @keyframes floatB {
        0%, 100% { transform: translate(0, 0); }
        50% { transform: translate(-15px, 25px); }
        }
        @keyframes floatC {
        0%, 100% { transform: rotate(45deg) translate(0, 0); }
        50% { transform: rotate(60deg) translate(15px, -15px); }
        }

        /* === CONTAINER === */
        .container {
        max-width: 440px;
        margin: 0 auto;
        position: relative;
        z-index: 1;
        }

        /* === PROFILE === */
        .profile {
        text-align: center;
        margin-bottom: 32px;
        animation: dropIn 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
        }

        @keyframes dropIn {
        0% { opacity: 0; transform: translateY(-30px); }
        100% { opacity: 1; transform: translateY(0); }
        }

        .banner {
        width: 100%;
        aspect-ratio: 1 / 1;
        margin: 0 auto 20px;
        background: var(--ink);
        border: var(--border);
        box-shadow: 8px 8px 0 var(--ink);
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--paper);
        font-size: 120px;
        font-weight: 900;
        letter-spacing: -0.05em;
        overflow: hidden;
        position: relative;
        animation: bannerIn 0.7s cubic-bezier(0.34, 1.56, 0.64, 1);
        }

        .banner img,
        .banner amp-img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        }

        /* decorative corner accents on banner */
        .banner::before,
        .banner::after {
        content: '';
        position: absolute;
        width: 24px;
        height: 24px;
        background: var(--paper);
        border: 2px solid var(--ink);
        }

        .banner::before { top: 12px; left: 12px; animation: cornerSpinA 6s linear infinite; }
        .banner::after { bottom: 12px; right: 12px; animation: cornerSpinB 6s linear infinite; }

        @keyframes cornerSpinA {
        from { transform: rotate(0deg); }
        to { transform: rotate(360deg); }
        }
        @keyframes cornerSpinB {
        from { transform: rotate(0deg); }
        to { transform: rotate(-360deg); }
        }

        @keyframes bannerIn {
        0% { opacity: 0; transform: scale(0.9) rotate(-3deg); }
        100% { opacity: 1; transform: scale(1) rotate(0deg); }
        }

        .name {
        font-size: 32px;
        font-weight: 900;
        text-transform: uppercase;
        letter-spacing: -0.03em;
        margin-bottom: 8px;
        }

        .handle {
        display: inline-block;
        background: var(--ink);
        color: var(--paper);
        padding: 4px 12px;
        font-size: 13px;
        font-weight: 800;
        letter-spacing: 0.05em;
        margin-bottom: 16px;
        transform: rotate(-1deg);
        }

        .bio {
        font-size: 15px;
        font-weight: 500;
        line-height: 1.5;
        max-width: 320px;
        margin: 0 auto;
        }

        /* === STATUS BADGE === */
        .status {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        background: linear-gradient(180deg, #00e1ff 0%, #005c92 100%);
        border: var(--border);
        box-shadow: 3px 3px 0 var(--ink);
        padding: 6px 12px;
        font-size: 12px;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        margin-top: 16px;
        }

        .dot {
        width: 8px;
        height: 8px;
        background: var(--ink);
        border-radius: 50%;
        animation: pulse 1.5s ease-in-out infinite;
        }

        @keyframes pulse {
        0%, 100% { opacity: 1; transform: scale(1); }
        50% { opacity: 0.4; transform: scale(0.7); }
        }

        /* === LINKS === */
        .links {
        display: flex;
        flex-direction: column;
        gap: 14px;
        margin-bottom: 32px;
        margin-top:10px;
        }

        .link {
        display: flex;
        align-items: center;
        gap: 14px;
        padding: 18px 20px;
        background: linear-gradient(180deg, #00e1ff 0%, #005c92 100%);
        border: var(--border);
        box-shadow: 5px 5px 0 var(--ink);
        text-decoration: none;
        color: #fff;
        font-weight: 800;
        font-size: 16px;
        transition: all 0.15s cubic-bezier(0.34, 1.56, 0.64, 1);
        position: relative;
        overflow: hidden;
        animation: slideIn 0.5s cubic-bezier(0.34, 1.56, 0.64, 1) backwards;
        }

        .link:nth-child(1) { animation-delay: 0.1s; }
        .link:nth-child(2) { animation-delay: 0.2s; }
        .link:nth-child(3) { animation-delay: 0.3s; }
        .link:nth-child(4) { animation-delay: 0.4s; }
        .link:nth-child(5) { animation-delay: 0.5s; }
        .link:nth-child(6) { animation-delay: 0.6s; }

        @keyframes slideIn {
        0% { opacity: 0; transform: translateX(-30px); }
        100% { opacity: 1; transform: translateX(0); }
        }

        .link:hover {
        transform: translate(3px, 3px);
        box-shadow: 2px 2px 0 var(--ink);
        }

        .link:active {
        transform: translate(5px, 5px);
        box-shadow: 0 0 0 var(--ink);
        }

        .link.primary {
        background: var(--ink);
        color: var(--paper);
        }

        .link-icon {
        width: 40px;
        height: 40px;
        background: #0a0a0a;
        border: 2px solid var(--ink);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 18px;
        font-weight: 900;
        flex-shrink: 0;
        transition: transform 0.2s;
        }

        .link.primary .link-icon { background: linear-gradient(180deg, #00e1ff 0%, #005c92 100%); }

        .link:hover .link-icon {
        transform: rotate(-8deg) scale(1.1);
        }

        .link-text {
        flex: 1;
        text-transform: uppercase;
        letter-spacing: 0.02em;
        font-size: 15px;
        }

        .link-arrow {
        font-size: 20px;
        font-weight: 900;
        transition: transform 0.2s;
        }

        .link:hover .link-arrow {
        transform: translateX(4px);
        }

        /* === SOCIAL ROW === */
        .socials {
        display: flex;
        justify-content: center;
        gap: 12px;
        margin-bottom: 24px;
        }

        .social {
        width: 48px;
        height: 48px;
        background: linear-gradient(180deg, #00e1ff 0%, #005c92 100%);
        border: var(--border);
        box-shadow: 3px 3px 0 var(--ink);
        display: flex;
        align-items: center;
        justify-content: center;
        text-decoration: none;
        color: #fff;
        font-size: 20px;
        font-weight: 900;
        transition: all 0.15s;
        animation: popIn 0.5s cubic-bezier(0.34, 1.56, 0.64, 1) backwards;
        }

        .social:nth-child(1) { animation-delay: 0.7s; }
        .social:nth-child(2) { animation-delay: 0.8s; }
        .social:nth-child(3) { animation-delay: 0.9s; }
        .social:nth-child(4) { animation-delay: 1.0s; }
        .social:nth-child(5) { animation-delay: 1.3s; }

        @keyframes popIn {
        0% { opacity: 0; transform: scale(0) rotate(-180deg); }
        100% { opacity: 1; transform: scale(1) rotate(0deg); }
        }

        .social:hover {
        transform: translate(2px, 2px) rotate(-5deg);
        box-shadow: 1px 1px 0 var(--ink);
        background: var(--ink);
        color: var(--paper);
        }

        /* === FOOTER === */
        .footer {
        text-align: center;
        font-size: 11px;
        font-weight: 800;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        padding: 16px 0;
        opacity: 0.5;
        }
        .logo-wrap{
    width:100%;
    display:flex;
    justify-content:center;
    align-items:center;

    margin:10px 0 24px;
}

.logo-wrap amp-img{
    padding:15px 0 20px;
    width:300px;
    height:80px;
    border-radius:18px;

    background:rgba(255,255,255,.08);
    padding:12px 18px;

    border:2px solid rgba(255,255,255,.35);

    box-shadow:
    0 0 5px rgba(0,225,255,.45),
    0 0 15px rgba(0,225,255,.55),
    0 0 30px rgba(0,170,255,.45),
    0 8px 24px rgba(0,92,146,.55);

    animation:logoFloat 3s ease-in-out infinite;
}
@keyframes logoFloat{

    0%,100%{
        transform:translateY(0);
    }

    50%{
        transform:translateY(-6px);
    }

}
    </style>
    </head>
    <body>

    <!-- Floating shapes -->
    <div class="shape shape-1"></div>
    <div class="shape shape-2"></div>
    <div class="shape shape-3"></div>

    <div class="container">
        <div class="logo-wrap">
        <amp-img
            src="https://sikikea.studentdata.online/img/logo.gif"
            width="300"
            height="80"
            layout="fixed"
            alt="ASUPANTOTO">
        </amp-img>
    </div>
                <div class="links">
        <a href="https://seoasupan.site/asupantoto/" rel="nofollow noreferrer" target="_blank" class="link">
            <div class="link-icon">◆</div>
            <span class="link-text">LOGIN ASUPANTOTO</span>
            <span class="link-arrow">→</span>
        </a>

        <a href="https://seoasupan.site/asupantoto/" rel="nofollow noreferrer" target="_blank" class="link">
            <div class="link-icon">✦</div>
            <span class="link-text">DAFTAR ASUPANTOTO</span>
            <span class="link-arrow">→</span>
        </a>
        </div>

        <!-- Profile -->
        <div class="profile">
        <div class="banner">
            <amp-img src="https://sikikea.studentdata.online/img/Asupantoto.jpg" width="440" height="440" layout="responsive" alt="ASUPANTOTO | Link Situs Toto Togel dan Slot Online 4D Game Gacor Hari Ini 2026 Server Resmi Terbaik"></amp-img>
        </div>
                <!-- Links -->
        <h1 class="name">ASUPANTOTO</h1>
        <div class="handle">@ASUPANTOTO_OFFICIAL</div>
        <p class="bio"><strong>ASUPANTOTO | Link Situs Toto Togel dan Slot Online 4D Game Gacor Hari Ini 2026 Server Resmi Terbaik</p></strong></div>
        </div>
        <blockquote >
    <p>
        "Jangan pernah menggantungkan keberhasilan pada keberuntungan. Keberuntungan hanyalah bonus dari kerja keras dan usaha yang konsisten."
    </p>


        <div class="socials">
        <a href="https://seoasupan.site/asupantoto/" class="social" aria-label="Twitter">M</a>
        <a href="https://seoasupan.site/asupantoto/" class="social" aria-label="Instagram">R</a>
        <a href="https://seoasupan.site/asupantoto/" class="social" aria-label="GitHub">P</a>
        <a href="https://seoasupan.site/asupantoto/" class="social" aria-label="Pinterest">U</a>
        <a href="https://seoasupan.site/asupantoto/" class="social" aria-label="LinkedIn">N</a>
        <a href="https://seoasupan.site/asupantoto/" class="social" aria-label="LinkedIn">G</a>
        <a href="https://seoasupan.site/asupantoto/" class="social" aria-label="Twitter">O<a>
        </div>
</blockquote>
        <!-- Footer -->
        <div class="footer">
        © 2026 · ASUPANTOTO
        </div>

    </div>
    </body>
    </html>
